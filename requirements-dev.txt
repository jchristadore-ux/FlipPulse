# Development / test dependencies (CI + local): the bot runtime, the onboarding
# service deps (its tests import onboarding.app), and pytest.
-r requirements.txt
-r onboarding/requirements.txt
pytest>=7.0.0
# Spreadsheet/workbook builders: business/build_model.py and
# marketing/blitz/build_run_the_blitz.py generate .xlsx files.
openpyxl>=3.1.0
