# Secrets / local env
.env
*.pem

# Bot state (lives on the Railway volume, never in git)
/data/
*_state.json
bucket_stats.json
status_snapshot.json
billing_state.json

# Onboarding submissions — contain ENCRYPTED customer secrets, never commit
onboarding/submissions/*.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# OS / editor
.DS_Store
.idea/
.vscode/
