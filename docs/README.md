# docs/

These two documents are copied unchanged from the `markeymachine` repo
(branch `main`):

- `TRADING_DOCTRINE.md` — the trading doctrine / rules the bot follows.
- `LADDER_STRATEGY.md` — the ladder strategy and the available `TRADING_FORMAT`
  options referenced by `.env.example`.

They are pending the code copy from `markeymachine` (see the repo README
"Code status" note). Do **not** copy anything from `markeymachine/dashboard/`.
